import { StyleSheet, Text, View } from 'react-native';

export default function LoginPage() {
  return (
    <Text style={{fontSize:40}}>Logowanie</Text>
  );
}

const styles = StyleSheet.create({
  
});
